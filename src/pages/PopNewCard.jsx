import PopNewCard from "../components/PopUps/PopNewCard/PopNewCard";

const PopNewCardPage = ({ setCardList }) => {
  return <PopNewCard setCardList={setCardList} />;
};

export default PopNewCardPage;
