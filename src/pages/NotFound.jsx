import styled from "styled-components";

export const Info = styled.h1`
  font-family: "Roboto", sans-serif;
  margin-top: 50px;
`;

const NotFoundPage = () => {
  return <Info>Страница не найдена скобка грусти(</Info>;
};

export default NotFoundPage;
